{
  "name": "smtm-emma",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npx playwright install chromium --with-deps",
    "start": "cd server && node index.js",
    "dev:server": "cd server && node --watch index.js",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
